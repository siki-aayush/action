name: "Checkout Testing"
description: "Testing the checkout function of github action"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      -uses: actions/checkout@v2
      -uses: actions/setup-node@v1
      -run: npm install -g bats
      -run: bats -v
